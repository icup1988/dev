# data/

Drop your Azure Cost Management Excel exports here.

The processing script (`scripts/process_excel.py`) picks the **last file alphabetically**,
so naming exports with the date (e.g. `CostManagement_rg-dataplatform-prd_2026-04-22.xlsx`) 
ensures the latest one is always used.

You can keep old files for reference — they won't interfere.
